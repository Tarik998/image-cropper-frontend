FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Expose development port
EXPOSE 4200

# Start development server (uses environment.ts with localhost:5001)
CMD ["npx", "ng", "serve", "--host", "0.0.0.0", "--port", "4200"]